[phases.install]
cmds = ['npm install --legacy-peer-deps']

[phases.build]
cmds = [
  'npx prisma generate',
  'npm run build:web'
]

[start]
cmd = 'caddy run --config /assets/Caddyfile --adapter caddyfile'
