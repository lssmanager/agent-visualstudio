/**
 * EditableFlowCanvas.tsx — LEGACY REDIRECT
 *
 * ⚠️  Este archivo fue la versión legacy del canvas. Tenía un import roto:
 *     import { FlowNodeCard } from './FlowNodeCard'  ← archivo inexistente
 *
 * La implementación canónica y activa vive en:
 *     apps/web/src/features/canvas/components/EditableFlowCanvas.tsx
 *
 * Este archivo redirige al canónico para no romper imports existentes.
 * Eliminar este shim una vez que todos los consumidores usen la ruta canónica.
 */
export { EditableFlowCanvas } from './components/EditableFlowCanvas';
