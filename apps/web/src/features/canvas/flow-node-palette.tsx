/**
 * flow-node-palette.tsx — LEGACY REDIRECT
 *
 * ⚠️  Este archivo era la paleta legacy con kinds PascalCase incompatibles
 *     con el sistema de tipos canónico (snake_case en canvas-utils.ts).
 *
 * La paleta canónica y activa es:
 *     apps/web/src/features/canvas/components/NodePalette.tsx
 *
 * Este archivo redirige al canónico para no romper imports existentes.
 */
export { NodePalette as FlowNodePalette } from './components/NodePalette';
